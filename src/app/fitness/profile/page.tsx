'use client';

import FitnessProfile from '../FitnessProfile';

// Standalone full-screen design (own radial background, plexus and bottom nav),
// so it renders without the NuraPageShell wrapper — matching Progress.
export default function FitnessProfilePage() {
  return <FitnessProfile />;
}
